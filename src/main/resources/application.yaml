server:
  servlet:
    context-path: /address
spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: admin
    password: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

address_service:
  addressFilePath: data/AS_ADDR_OBJ.XML
  hierarchyFilePath: data/AS_ADM_HIERARCHY.XML